(ns sicp.part2_test
  (:require [clojure.test :refer :all]
            [sicp.part2 :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1))))

(comment
  ;; insta-repl test in lighttable
  (run-tests)
)
